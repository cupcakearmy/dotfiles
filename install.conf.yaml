- defaults:
    link:
      create: true
      relink: true
      force: true

- clean: ["~"]

- link:
    ~/.gitconfig: files/gitconfig
    ~/.vim: files/vim
    ~/.vimrc: files/vimrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
